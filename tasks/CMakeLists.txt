# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(servo_dynamixelTaskLib)
ADD_LIBRARY(${SERVO_DYNAMIXEL_TASKLIB_NAME} SHARED 
    ${SERVO_DYNAMIXEL_TASKLIB_SOURCES})
add_dependencies(${SERVO_DYNAMIXEL_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SERVO_DYNAMIXEL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SERVO_DYNAMIXEL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SERVO_DYNAMIXEL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SERVO_DYNAMIXEL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SERVO_DYNAMIXEL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SERVO_DYNAMIXEL_TASKLIB_HEADERS}
    DESTINATION include/orocos/servo_dynamixel)

